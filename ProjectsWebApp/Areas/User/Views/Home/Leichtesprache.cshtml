@model LeichteSpracheContent

@{
    ViewData["Title"] = "Leichte Sprache";
} 

<div class="mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            @Html.Raw(Model.ContentHtml)  <!-- Dynamically loads saved content -->
        </div>
    </div>
</div>

@model IEnumerable<ProjectsWebApp.Models.Fachgruppen>

@{
    ViewData["Title"] = "Fachgruppe bearbeiten";
}

<div class="mt-5">
    <div class="row">
        <div class="col-12">

            <h3 class="text-center mb-4">@ViewData["Title"]</h3>
            <div class="d-flex justify-content-end mb-3">
                <a href="@Url.Action("Create", "Fachgruppen", new { area = "Admin" })" class="btn btn-primary addBtn">
                    <i class="fas fa-plus"></i> 
                </a>
            </div>

            <table class="table table-bordered table-hover shadow-sm">
                <thead class="table-dark">
                    <tr>
                        <th>Fachgruppe</th>
                       
                        <th style="width: 150px;">Aktionen</th> <!-- Adjusted width for the Actions column -->
                    </tr>
                </thead>
                <tbody>
                    @foreach (var Fachgruppen in Model.OrderBy(u => u.DisplayOrder))
                    {
                        <tr>
                            <td>@Fachgruppen.Name</td>
                          
                            <td>
                                <div class="d-flex justify-content-between">
                                    <a href="@Url.Action("Edit", "Fachgruppen", new { id = Fachgruppen.Id, area = "Admin" })" class="btn btn-warning rounded-5 mx-1">
                                        <i class="bi bi-pencil-square"></i>
                                    </a>
                                    <a href="@Url.Action("Delete", "Fachgruppen", new { id = Fachgruppen.Id, area = "Admin" })" class="btn btn-danger rounded-5 mx-1">
                                        <i class="bi bi-trash-fill"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
